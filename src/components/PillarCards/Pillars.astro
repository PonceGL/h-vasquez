---
import { PILLARS } from "../../constants/company";
import Card from "./Card.astro";
---

<section class="w-full flex justify-center items-center relative">
    <ul class="w-full flex flex-wrap justify-evenly items-center gap-10 -translate-y-20">
        {PILLARS.map((pillar) => (
            <Card {...pillar} />
        ))}
    </ul>
</section>